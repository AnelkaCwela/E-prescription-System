@model DosageFormModel
@{
    ViewData["Title"] = "Create Dosage Form";
}
<h4 class="text-center">Create Dosage Form</h4>
<hr />
@if (ViewBag.Data != null)
{
    <h4 class="text-center alert alert-success alert-heading"> @ViewBag.Data</h4>
}
<form asp-action="CreateDosageForm" method="post">
    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
    <div class="row mb-4">
        <div class="col">
            <div class="form-outline">
                <label class="form-label" for="form3Example1">Dosage Form</label>
                <input asp-for="DosageFormName" placeholder="Dosage Form" required id="form3Example1" class="form-control" />

                <span asp-validation-for="DosageFormName" class="text-danger"></span>
            </div>
        </div>
    </div>
    <!-- Submit button -->
    <button type="submit" class="btn btn-primary btn-block mb-4">Submit</button>
</form>
